export const APP_NAME = "Music Player";

export const DEFAULT_SEARCH = "arijit";

export const PAGINATION_LIMIT = 20;

export const STORAGE_KEYS = {
  QUEUE: "music_queue",
};

export const AUDIO = {
  UPDATE_INTERVAL: 500,
};